var quick_draw_data_set = aircraft carrier, airplane, alarm clock, ambulance, angel, animal migration, ant, anvil;

function setup() {
    canvas = createCanvas(500, 500);
    canvas.position(537, 320);
    background("white");
}
// Can't understand after this
function updateCanvas() {
    background("white");
    random_number = Math.floor(quick_draw_data_set);
}